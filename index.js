// Arrays

const numbers = [3, 4];

// Adding elements to an array
// to the End, Beginning or Middle

console.log(numbers);

// End
numbers.push(5, 6)
console.log(numbers);

//Beginning
numbers.unshift(9, 0);
console.log(numbers);

//Middle
numbers.splice(2, 0, 'a', 'b');
console.log(numbers);